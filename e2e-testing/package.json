{
  "name": "e2e-testing",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "local": "npx cypress run --headed --browser chrome -c baseUrl=http://localhost:8000",
    "local-interactive": "npx cypress open --e2e --browser chrome -c baseUrl=http://localhost:8000",
    "dev": "npx cypress run --headed --browser chrome -c baseUrl=https://telotawebdev.bbaw.de/ccdb_edit/",
    "dev-interactive": "npx cypress open --e2e --browser chrome -c baseUrl=https://telotawebdev.bbaw.de/ccdb_edit/"
  },
  "author": "Marcus Lampert",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.2.0",
    "path": "^0.12.7",
    "typescript": "^4.9.4"
  }
}
